Isso é uma sintaxe básica para qualquer código Kotlin

Para declarar uma variável utilizamos `Var` e uma variável constante usamos `Val`
Para não haver conflitos no  código, utilize apenas `Var` se você tiver certeza que a variável vai ser modificada futuramente